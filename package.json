{
  "name": "argenmap",
  "version": "0.0.0",
  "private": true,
  "description": "Visor de mapas del Instituto Geografico Nacional",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:versioned": "node scripts/versioned-build.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.8"
  },
  "engines": {
    "node": ">=20"
  }
}
